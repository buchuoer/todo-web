# 🎯 快速测试参考卡

## 🚀 立即开始
👉 打开浏览器访问: **http://localhost:5173**

---

## ⚡ 最快验证方式 (3 分钟)

### 测试 1: 待办 Enter 键
```
1. 切换到"待办"标签
2. 输入: "快速测试"
3. 按 Enter 一次
4. ✅ 确认: 只有 1 个待办项被创建
```

### 测试 2: 日志 Enter 键
```
1. 切换到"日志"标签
2. 输入: "这是一条日志信息"
3. 按 Enter 一次
4. ✅ 确认: 只有 1 条日志被创建
```

### 测试 3: Shift+Enter 换行
```
1. 在日志框输入: "第一行"
2. 按 Shift+Enter (不是普通 Enter)
3. 输入: "第二行"
4. 按 Enter (普通)
5. ✅ 确认: 日志显示两行内容，成功提交
```

### 测试 4: 日志关键词检索
```
1. 在日志页创建两条不同内容的日志
2. 在日志搜索框输入其中一条日志里的关键词
3. ✅ 确认: 只显示命中的日志
4. 清空搜索
5. ✅ 确认: 完整日志列表恢复
```

### 测试 5: 单条日志 AI 建议卡
```
1. 在任意一条日志下点击"深入聊聊"
2. ✅ 确认: 该日志下出现加载中的 AI 卡片
3. 等待返回
4. ✅ 确认: 只在该日志下显示 AI 分析结果，不混入日志主列表
5. 点击"联网重答"
6. ✅ 确认: 卡片更新为联网结果，并显示联网标记
7. 在卡片底部继续追问
8. ✅ 确认: 对话以多轮消息流追加
```

---

## 📊 完整测试清单 (5-10 分钟)

| # | 测试 | 操作 | 期望结果 | ✅ |
|---|------|------|--------|-----|
| 1 | 待办 Enter | 输入→按 Enter 1 次 | 创建 1 个 | ☐ |
| 2 | 待办连续 Enter | 输入→按 Enter 3 次 | 创建 1 个（不是 3 个） | ☐ |
| 3 | 日志 Enter | 输入→按 Enter 1 次 | 创建 1 条 | ☐ |
| 4 | Shift+Enter | 输入→Shift+Enter→输入→Enter | 两行日志 | ☐ |
| 5 | 字符限制 | 输入 "短"→Enter | 不创建 | ☐ |
| 6 | 日志搜索 | 输入关键词 | 只显示命中日志 | ☐ |
| 7 | AI 建议卡 | 点击单条日志下操作 | 显示对应 AI 结果 | ☐ |
| 8 | 联网重答 | 点击“联网重答” | 显示联网模式结果 | ☐ |
| 9 | 纯空格 | 输入 5 个空格→Enter | 不创建 | ☐ |
| 10 | 连续日志 | 日志 1→Enter→日志 2→Enter | 创建 2 条 | ☐ |

---

## 🔧 代码修改验证

打开浏览器开发者工具 (F12) → Sources → 搜索 App.tsx

### 验证点 1️⃣: 待办输入框 (行 387-395)
```tsx
onKeyDown={(e) => {
  if (e.key === 'Enter') {
    e.preventDefault()  // ✅ 关键：防止默认行为
    if (input.trim()) {
      addTodo(input, '生活', deadline || undefined)
    }
  }
}}
```

### 验证点 2️⃣: 日志处理函数 (行 284-302)
```tsx
const handleLogKeyDown = (e: React.KeyboardEvent) => {
  if (e.key === 'Enter' && !e.shiftKey) {
    e.preventDefault()  // ✅ 防止默认换行
    // ... 逻辑
  }
}
```

### 验证点 3️⃣: 日志 textarea (行 526)
```tsx
onKeyDown={handleLogKeyDown}  // ✅ 改为 onKeyDown
```

### 验证点 4️⃣: Placeholder 提示 (行 523)
```tsx
placeholder="记录想法...（Shift+Enter 换行）"
// ✅ 日志输入以普通记录为主，不再依赖 @AI 提示
```

---

## 💭 常见问题

**Q: 按 Enter 时还是出现两条？**
A: 检查浏览器缓存，按 Ctrl+Shift+Delete 清除缓存后重试。

**Q: Shift+Enter 没有换行？**
A: 确保按的是 Shift+Enter（不是 Alt+Enter），日志框支持多行。

**Q: AI 建议卡没有响应？**
A: 现在 AI 请求走 Cloudflare Pages Functions。先检查浏览器 Network 是否能访问 `/api/ai/*`，再检查 Cloudflare 中的 `OPENAI_API_KEY`、`OPENAI_BASE_URL`、`DEEPSEEK_API_KEY` 是否已配置。

**Q: 日志框有红色边框或错误？**
A: 打开浏览器控制台 (F12) 查看具体错误信息。

---

## 📸 截图标记 (可选)

如发现问题，可保存以下截图：
- [ ] 浏览器地址栏（确认 http://localhost:5173）
- [ ] 待办项列表（显示重复）
- [ ] 控制台错误信息 (F12 → Console)
- [ ] Network 请求（如有 API 调用）

---

## ✅ 测试完成后

### 如果所有测试 ✅ 通过:
```
✨ 阶段 1 完成！
📝 下一步: 阶段 2 - 增强输入体验
   - 添加字数统计
   - 草稿自动保存
   - 改进 AI 错误提示
```

### 如果发现 ❌ 问题:
```
1. 记录问题详情（上表）
2. 提供重现步骤
3. 共享浏览器和 OS 信息
4. 创建新问题或修改代码
```

---

## 📞 需要帮助?

- 📖 详细文档: 查看 TEST_CHECKLIST.md
- 🔍 代码调试: F12 打开开发者工具，检查 Console 和 Network
- 💻 代码位置: `/Users/clz/Desktop/大三下/gemini-test/src/App.tsx`
- 🌐 服务器: http://localhost:5173

**祝你测试愉快！** 🚀
